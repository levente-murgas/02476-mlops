# def test_train():
#     load_dotenv()  # take environment variables from .env file
#     assert os.getenv("WANDB_ENTITY") is not None, "WANDB_ENTITY is not set in .env"
#     assert os.getenv("WANDB_PROJECT") is not None , "WANDB_PROJECT is not set in .env"
#     assert os.getenv("WANDB_API_KEY") is not None   , "WANDB_API_KEY is not set in .env"

#     config_path = os.path.join(_PROJECT_ROOT, "configs", "config.yaml")
#     assert os.path.exists(config_path)
